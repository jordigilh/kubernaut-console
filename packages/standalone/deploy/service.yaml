apiVersion: v1
kind: Service
metadata:
  name: kubernaut-console
  labels:
    app.kubernetes.io/name: kubernaut-console
    app.kubernetes.io/component: frontend
    app.kubernetes.io/part-of: kubernaut
spec:
  selector:
    app.kubernetes.io/name: kubernaut-console
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
