# API Frontend upstream URL (used by Vite dev server proxy)
# Override to point at a remote AF instance or custom port
VITE_API_UPSTREAM=http://localhost:8443

# Enable mock A2A mode for frontend-only development (no backend needed)
# Set to "true" to use canned mock responses instead of real AF
VITE_MOCK_A2A=false
